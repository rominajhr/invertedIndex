# 📚 Inverted Index with B-Tree Storage  
ساخت اینورتد ایندکس با ذخیره‌سازی ترم‌ها در B-Tree + نمایش گرافیکی

این پروژه یک سیستم ساده "Information Retrieval" پیاده‌سازی می‌کند که:

- اسناد را به صورت **رندوم و با واژگان بسیار متنوع** تولید می‌کند  
- برای هر سند، یک **Inverted Index** می‌سازد  
- تمام ترم‌ها را داخل یک **B-Tree** ذخیره می‌کند  
- امکان انجام **جستجو** دارد  
- از درخت یک **ASCII visualization** رسم می‌کند  

هدف پروژه: نزدیک شدن به ساختار اصلی موتورهای جستجو و تمرین ساخت ایندکس‌های کارآمد.

---

## ✨ ویژگی‌ها

### 🔹 تولید اسناد بزرگ و واقعی  
هر سند حدود **۳۰۰۰ توکن** دارد و شامل هزاران واژه‌ی یونیک (با generator اختصاصی).

### 🔹 ساخت Inverted Index  
برای هر term، لیست داکیومنت‌هایی که در آن ظاهر شده‌اند ذخیره می‌شود.

### 🔹 پیاده‌سازی B-Tree کامل  
- درج (`insert`)  
- تقسیم نودها (`split`)  
- ذخیره ترم‌ها به صورت **sorted**  
- مناسب ذخیره‌سازی ایندکس‌های بزرگ  

### 🔹 نمایش B-Tree  
- حالت **ASCII** (برای ترمینال)
- حالت **Graph** با matplotlib

### 🔹 جستجو  
با تابع `search(term)` می‌توان داکیومنت‌های دارای آن term را یافت.

---

## 📁 ساختار پروژه

